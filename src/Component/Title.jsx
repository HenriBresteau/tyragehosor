import React from "react";

const Title = ({ children }) => {
    return (
        <h2 className="text-7xl font-gilroy font-bold mb-4 tracking-tight">
            {children}
        </h2>
    );
};

export default Title;
